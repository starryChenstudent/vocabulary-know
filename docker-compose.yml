services:
  vocabulary-know:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/vocabulary.db
      - OCR_ENGINE=auto
    env_file:
      - .env
